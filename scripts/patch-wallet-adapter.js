/**
 * This script is now deprecated and replaced by apply-font.cjs
 * Keeping this file for reference but should not be used anymore.
 * 
 * Note: This script previously patched the Solana wallet adapter styles
 * to use Orbitron font, but we now handle this through inline styles
 * in WalletWrapper.js and styles/wallet.css.
 */
console.log('⚠️ This script is deprecated. Font overrides are now handled in WalletWrapper.js and styles/wallet.css');
console.log('✅ No action needed - Orbitron font will be applied automatically');